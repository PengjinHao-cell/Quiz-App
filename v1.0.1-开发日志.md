# v1.0.1 开发日志

> 版本：v1.0.1  
> 日期：2026-05-29  
> 标签：`git tag v1.0.1`  
> 范围：云端同步 + UI 改进 + Bug 修复

---

## 一、本次实现的功能

### 1. 用户数据云端同步（核心功能）

- **新增 3 个数据库表**：`WrongAnswer`（错题本）、`Favorite`（收藏）、`StudyHistory`（学习记录）
- **新增 9 个 Sync API 端点**（`/api/sync/*`），全部 `@login_required` + CSRF 防护
- **前端同步层**：`syncWrongBookToServer()` / `syncFavoritesToServer()` / `syncHistoryToServer()`
- 登录用户操作自动推送到服务器，访客模式不受影响

### 2. UI 改进

- **下拉菜单动画**：从 `display:none/block` 改为 `opacity + transform + visibility` 过渡，实现从上往下滑出效果
- **成绩分享图片化**：点击「保存为图片」动态加载 html2canvas，生成长图下载
- **上传模式扩展**：文件上传新增「仅上传（不跳转）」选项，AI 解析新增「不跳转」勾选框
- **用户协议底部**：添加联系邮箱 + GitHub 链接

### 3. Bug 修复

- 结果页因 html2canvas CDN 阻塞 DOMContentLoaded 导致卡住 → 改为按需动态加载
- 修复了 `quiz.html` / `reading.html` 嵌套 `<script>` 标签的渲染 bug
- 修复了 `result.html` 重复声明 `RESULT_BANK_ID` 的问题
- 给 `style.css` 和所有 JS 文件统一添加版本号参数，解决浏览器缓存问题

---

## 二、踩坑路线图（做错的和做对的）

### ❌ 踩坑 1：双向同步，服务器为权威源

**做法**：`loadServerData()` 中，合并时删除本地有但服务器没有的项。

**结果**：用户刚注册、服务器还没数据时 → 本地全部错题被清空。

**教训**：不能假设服务器数据完整。首次登录/同步未完成时，服务器空数据不应被视为"应该删掉"。

### ❌ 踩坑 2：双向同步 + 守卫条件

**做法**：只在服务器有数据时才做双向同步。

**结果**：解决了空数据清空问题，但删除操作是异步的 → 用户刷新页面时 DELETE 还没到服务器 → loadServerData 从服务器拉回被删的数据。

**教训**：异步删除的竞态条件，仅在服务器端做延迟处理不够。

### ❌ 踩坑 3：删除黑名单

**做法**：在 localStorage 维护 `_deletedSyncKeys` 黑名单，删除时标记，合并时跳过。

**结果**：理论上正确，但 `/user` 页面（user.html）有自己的删除函数，完全没有走黑名单机制。`deleteFavoriteItem` 直接操作 localStorage、`deleteHistoryItem` 用索引定位（删除后索引漂移）、`clearAll*` 清空时也不调用服务器端清理。

**教训**：同一个功能在两个页面有各自的实现，容易漏掉同步更新。应该把删除逻辑统一到 utils.js 中。

### ❌ 踩坑 4：版本号没更新

**做法**：改了 JS 代码但没更新 `?v=` 版本号。

**结果**：浏览器一直跑旧版 JS，用户反馈"没有实现"。中间反复重启服务器都没用。

**教训**：改 JS 文件后必须同步更新所有模板中的版本号参数，否则缓存会掩盖一切改动。

### ✅ 最终方案：单向同步（本地 → 服务器）

**做法**：`loadServerData()` 完全不做数据合并。本地永远是权威源，服务器只做备份。

**结果**：删除永不回弹。简单、可预测、容易推理。

---

## 三、做得好 vs 做得不好

### 👍 做得好

- **CSRF 防护**：早期就加上了 `X-Requested-By: QuizApp` 头部校验，避免了后续加端点时的安全遗漏
- **按需加载**：html2canvas 改为点击按钮时才加载，不阻塞页面初始化
- **版本号统一升级**：发现问题后对所有模板做了统一处理，一次性解决了缓存问题
- **代码审查**：用 review skill 检查发现了嵌套 `<script>` bug 等自己没注意到的问题
- **数据模型设计**：三个表的唯一约束、to_dict 方法、字段类型都合理

### 👎 做得不好

- **过度设计**：一开始追求完美的双向同步，花了太多时间在黑名单、守卫条件等复杂机制上。最终方案反而是最简单的单向同步
- **没有一次性覆盖所有页面**：`/user` 页面有独立的 JS 实现，改 `app.js` 和 `utils.js` 时完全没意识到还需要改 `user.html`
- **缓存意识不足**：改了 JS 没第一时间更新版本号，导致多次"修复无效"的假象
- **没有加版本号自动化**：每次手动改版本号容易遗漏，应该用自动化工具或统一版本变量
- **删除历史用索引**：user.html 的 `deleteHistoryItem(idx)` 用数组索引定位记录，删除后索引偏移会导致删错行。应该用记录的唯一 `id`

---

## 四、待改进

1. **跨设备数据同步**：当前单向同步（本地→服务器），换设备后需要手动拉取。后续可加"从云端恢复"按钮
2. **唯一操作入口**：删除、收藏等操作应统一到 utils.js，各页面只负责 UI 渲染，不直接操作数据
3. **版本号自动化**：Flask 的 `url_for('static', filename='xxx.js', v=VERSION)` 中的 VERSION 应该从环境变量或文件 hash 自动生成
4. **删除历史用 ID 而非索引**：user.html 的 deleteHistoryItem 还有索引漂移的隐患
5. **测试**：9 个 Sync API 端点没有自动化测试

---

# v1.0.2 开发日志 — 邮箱验证码 + 管理员面板

> 版本：v1.0.2  
> 日期：2026-05-29  
> 标签：`git tag v1.0.2`  
> 范围：邮箱功能打通 + 管理员后台 + 全量 Bug 修复

---

## 一、本次实现的功能

### 1. 邮箱验证码（重头戏）

- **购买域名** `quizmasterprogram.top`，在腾讯云 DNSPod 配置 DNS
- **注册 Resend** 并验证域名，配置 DKIM / SPF / DMARC / MX 四条 DNS 记录
- **API Key 踩坑**：旧 Key 没有新域名的发信权限，重新生成后解决
- **HTTP 库踩坑**：Python `urllib.request` 在 Railway 上发 Resend API 一直返回 403 error code: 1010（同域名同 Key，curl 正常但 Python 不行）。排查半天后换用 `requests` 库解决问题
- **QQ 邮箱被拦截**：域名太新（当天注册），QQ 邮箱拒收。Gmail 和 yeah.net 正常
- **Resend suppression list**：QQ 邮箱被 Resend 自动加入黑名单，需手动移除

### 2. 管理员后台（第 2 轮实现）

- User 表加 `is_admin` 字段，带自动迁移（兼容已有 PostgreSQL 数据库）
- `/admin` 页面：5 张统计卡片 + 用户表格 + 重置密码/删除操作
- 删除密码安全加固：未设置 DELETE_PASSWORD 时自动降级为管理员鉴权

### 3. Bug 修复

- DELETE_PASSWORD 空值漏洞修复
- 考试倒计时 `alert()` 阻塞改为 Toast + `setTimeout`
- 数据库自动迁移（`_run_migration()` 检测并添加新列）
- `from` 地址格式兼容性处理

---

## 二、踩坑路线图（邮箱部分）

| 步骤 | 遇到问题 | 解决 |
|------|---------|------|
| 1. 注册 Resend | API Key 403 1010（旧 Key 无权限） | 生成新 API Key ✅ |
| 2. 配置 DNS | Resend Verify 不通过 | 等 DNS 生效后解决 ✅ |
| 3. Python 发信 | `urllib.request` 403（curl 正常） | 换成 `requests` 库 ✅ |
| 4. QQ 邮箱 | 收不到（域名太新被拒） | 等域名审核通过 ✅ |
| 5. Resend 黑名单 | QQ 邮箱被自动加入 suppression list | 手动移除 ✅ |

---

## 三、经验教训

- **邮箱是最难搞的部分**：域名注册、DNS 配置、API 兼容性、反垃圾策略——每一层都可能卡住
- **curl 正常 ≠ Python 正常**：同样 URL、同样 Header、同样 Body，`urllib` 和 `curl` 行为不同。遇到这种问题果断换库
- **域名信誉度需要时间**：新注册的域名发 QQ 邮箱大概率被拒，这是正常现象，等几天就好
- **API Key 要重新生成**：在 Resend 加了新域名后，旧 API Key 不一定自动获得新域名的权限

---

## 四、待改进（延续）

1. ~~题库持久化：迁移到 PostgreSQL~~ ✅ **v1.1.0 已解决**
2. **邮箱验证码备用通道**：域名审核通过后 QQ 邮箱自然能收，无需额外改动


---

# v1.1.0 开发日志 — 题库持久化到 PostgreSQL

> 版本：v1.1.0  
> 日期：2026-05-29  
> 标签：`git tag v1.1.0`  
> 范围：题库从 JSON 文件迁移到 PostgreSQL，统一版本号管理

---

## 一、本次改动

### 1. 题库持久化（核心）

**问题**：题库存在 `data/*.json` 文件，Railway 每次 Redeploy 重建文件系统，所有上传的题库全部丢失。

**解决方案**：新增 `QuestionBank` 数据库表，所有题库直接存到 PostgreSQL。

| 改前 | 改后 |
|------|------|
| `data/{bank_id}.json` 文件读写 | `question_banks` 表，`data_json` 字段存完整 JSON |
| `load_bank_list()` 遍历磁盘文件 | `QuestionBank.query.all()` 查数据库 |
| `load_bank()` 读 JSON 文件 | `QuestionBank.query.get(bank_id)` 查数据库 |
| `save_bank()` 写 JSON 文件 | `db.session.add(qb)` 写数据库 |
| 部署后数据丢失 | **数据持久化** ✅ |

**迁移逻辑**：启动时自动扫描 `data/*.json` 导入到数据库（已导入的跳过）。

### 2. 统一版本号

新增 `version.py`，所有模板通过 `{{ VERSION }}` / `{{ VERSION_NAME }}` 引用，以后改版本只改一个文件。

```python
# version.py — 唯一的版本号源
VERSION = "1.1.0"
VERSION_NAME = "v1.1.0"
```

---

## 二、涉及文件

| 文件 | 改动 |
|------|------|
| `models.py` | 新增 `QuestionBank` 表 |
| `version.py` | **新文件** — 统一版本号 |
| `app.py` | 5 个核心函数改为数据库读写 + 迁移 + context_processor 注入版本 |
| `templates/*.html` | 硬编码版本号改为 `{{ VERSION_NAME }}` |

---

## 三、验证结果

```
📦 迁移: 已导入 6 个题库到数据库
✅ 题库列表: 5 个（1个vocab无题目被过滤）
  📄 2026年上半年入党积极分子培训班 (296题)
  📄 CET-6 阅读理解 (5题)
  📄 Python基础示例题库（内置）(10题)
  📄 科目一 (65题)
  📄 英文缺词填空50题 (50题)
```
